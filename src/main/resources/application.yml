spring:
  jackson:
    serialization:
      fail-on-empty-beans: false
  servlet:
    multipart:
      maxFileSize: 5MB
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
  datasource:
    url: jdbc:mariadb://localhost:3306/blog?serverTimezone=UTC
    username: root
    password: 357951
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
